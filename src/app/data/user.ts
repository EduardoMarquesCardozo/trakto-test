export interface User {
    email: string;
    password: string;
}

export interface UserData {
    email: string;
    password: string;
    firstname: string;
    can_authenticate: string;
    access_token: string;
}
  